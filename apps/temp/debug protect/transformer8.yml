id: transformer8
type: function
subtype: Function
template:
  ordered:
    - funcBody: >-
        // let data = {{formatDataAsArray(metricsOverTime.data)}};

        // let historyData = {{formatDataAsArray(lifecycleFunnel.data)}};

        // let currentstages = _.keyBy(_(data).groupBy("stage").map((group,
        stage) => ({ stage: stage, count: group.length})).value(), 'stage')

        // let history = _.keyBy(_(historyData).groupBy("newvalue").map((group,
        newvalue) => ({ newvalue: newvalue, count: group.length})).value(),
        'newvalue')

        // return {currentstages, history} // Note: we'll have to reformat the
        metricsOverTime, as that is affected by selected lifecycle stages
    - value: ''
createdAt: 2022-12-02T02:31:16.064Z
folder: unused
